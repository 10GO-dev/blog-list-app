{
  "name": "blog_list",
  "version": "1.0.0",
  "description": "Blog List App",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node --env-file=.env index.js",
    "dev": "cross-env NODE_ENV=development nodemon --exec 'node --env-file=.env' index.js",
    "test": "cross-env NODE_ENV=test node --env-file=.env --test --test-concurrency=1",
    "test-only": "cross-env NODE_ENV=test node --env-file=.env --test --test-only"
  },
  "author": "Diego Peralta",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^9.0.2",
    "lodash": "^4.17.21",
    "mongoose": "^8.6.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.10.0",
    "@stylistic/eslint-plugin-js": "^2.8.0",
    "cross-env": "^7.0.3",
    "eslint": "^9.10.0",
    "globals": "^15.9.0",
    "nodemon": "^3.1.4",
    "supertest": "^7.0.0"
  }
}
